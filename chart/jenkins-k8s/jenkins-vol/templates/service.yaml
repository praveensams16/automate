apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkins-vol.fullname" . }}
  labels:
    {{- include "jenkins-vol.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jenkins-vol.selectorLabels" . | nindent 4 }}
