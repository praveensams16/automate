apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jenkins-vol.fullname" . }}-test-connection"
  labels:
    {{- include "jenkins-vol.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jenkins-vol.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
